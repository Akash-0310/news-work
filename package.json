{
  "name": "newsflow",
  "private": true,
  "version": "0.1.0",
  "description": "NewsFlow — daily news aggregation platform for India and the world",
  "workspaces": [
    "server",
    "web"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "dev": "npm-run-all --parallel dev:server dev:web",
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "dev:worker": "npm run dev:worker --workspace server",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "infra:up": "docker compose up -d postgres redis",
    "infra:down": "docker compose down",
    "setup": "npm run infra:up && npm run db:deploy && npm run db:seed",
    "db:generate": "npm run db:generate --workspace server",
    "db:migrate": "npm run db:migrate --workspace server",
    "db:deploy": "npm run db:deploy --workspace server",
    "db:seed": "npm run db:seed --workspace server",
    "db:studio": "npm run db:studio --workspace server",
    "test": "npm run test --workspace server",
    "lint": "npm run lint --workspace server && npm run lint --workspace web",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  }
}
